<section class='portfolio'>
  <div class='portfolio-title'>
    <h1>
      Portfolio
    </h1>
  </div>
  <section class='content-boxes'>
    {{#each contentBoxes as |contentBox index|}}
      {{content-box model=contentBox}}
    {{/each}}
  </section>
</section>
{{outlet}}
